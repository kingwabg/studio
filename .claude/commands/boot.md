세션 부팅. 다음 순서로 수행:

1. docs/playbooks/agent-protocol.md 를 읽는다 (전문 — 짧다).
2. 거기 "부팅" 규칙대로 멈춘다 — 다른 플레이북/로그는 지금 읽지 않는다.
3. `git log --oneline -5` 와 `git status -sb` 로 현재 상태를 파악한다.
   최근 3일 커밋에서 docs/design 수가 feat/fix 수를 넘으면 보고에
   "⚠ 메타작업 비율 N:M — 오늘은 제품 커밋 우선 권장" 1줄을 포함한다.
4. 한 줄 배지(`▸ 모델 · 밀도 — 이유`)와 함께 "부팅 완료 + 현재 상태 요약 + 다음 착수 후보(docs/product-spec.md 로드맵 P0 기준)"를 보고한다.

$ARGUMENTS 가 있으면 그것을 오늘의 작업 목표로 받아 위 보고에 착수 계획(파일·검증 방법)을 포함한다.
