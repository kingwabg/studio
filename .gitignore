node_modules
dist
.DS_Store
.env
.env.local
.verify/
# .claude는 로컬 설정만 무시 — 팀 공용 슬래시 명령(commands)은 추적한다
# (주의: 디렉터리 자체를 .claude/ 로 무시하면 하위 재포함(!)이 불가능 — 반드시 /* 패턴 유지)
.claude/*
!.claude/commands/
# 개발용 임시 로그 (codex/vite 프로세스 stdout·stderr)
.codex-vite-*.log

# rhwp-studio 입양본 — 생성물 제외 (소스만 추적, wasm은 sync:rhwp가 @rhwp/core에서 공급)
rhwp-studio/node_modules
rhwp-studio/dist
/pkg
